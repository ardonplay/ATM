create table test (
                      id serial not null,
                      title varchar(1000) not null,
                      quantity numeric not null
);

insert into test(
    title, quantity
)  values ('ogoщпщпщпщп', 10);
